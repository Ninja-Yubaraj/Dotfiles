#!/usr/bin/env bash
#
# Dependencies: pywal, betterlockscreen

wal -q -i "$1"

# Import the colors
. "$HOME/.cache/wal/colors.sh"

# Set background for betterlockscreen
betterlockscreen -u $wallpaper
